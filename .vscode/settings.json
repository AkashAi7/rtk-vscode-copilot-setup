{
  "github.copilot.chat.codeGeneration.instructions": [
    {
      "text": "Always prefix terminal commands with `rtk` (e.g., `rtk npm test` or `rtk git status`) to compress output and save tokens. If rtk is not installed, remind the user to install it from https://github.com/rtk-ai/rtk."
    }
  ]
}